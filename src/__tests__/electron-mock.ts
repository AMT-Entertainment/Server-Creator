export const contextBridge = {
  exposeInMainWorld: () => {},
};

export const ipcRenderer = {
  invoke: async () => {},
  on: () => {},
  removeListener: () => {},
};

export const IpcRendererEvent = class {};
